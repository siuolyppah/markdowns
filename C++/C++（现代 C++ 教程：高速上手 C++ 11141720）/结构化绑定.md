- 例如：

  ```CPP
  #include <iostream>
  #include <tuple>
  
  std::tuple<int, double, std::string> foo() {
    return std::make_tuple(1, 2.2, "hello");
  }
  
  int main() { 
      auto [x, y, z] = foo();
      std::cout << x << "," << y << "," << z <<std::endl;
  }
  ```

- 